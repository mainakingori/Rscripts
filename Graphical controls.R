dose <- c(20, 30, 40, 45, 60)
drugA <- c(16, 20, 27, 40, 60)
drugB <- c(15, 18, 25, 31, 40)
k=data.frame(dose,drugA,drugB)
k
factor(data.frame(dose,drugA,drugB))
opar <- par(no.readonly=TRUE)
par(pin=c(2, 3),mar=c(5,4,4,1))
par(lwd=2, cex=1.5)
par(cex.axis=.75, font.axis=5)
plot(dose, drugA, type="b", pch=9, lty=6, col="purple")
plot(dose, drugB, type="b", pch=3, lty=6, col="blue", bg="green")
attach(k)
abline(lm(dose~drugA+drugB))
par(opar)